Bracket usage
```Cpp
if (/* condition */) {
  /* code stuff */
} else {
  /* code stuff */
}
```

Space padding

`if ( /* condition */)` ← bad

`if (/* condition */ )` ← bad

`if (/* condition */)` ← good

